language: python
python:
- 3.4

#script
  #before script
  #script

jobs:
  include:
  - stage: build
    script: echo 5
    script: echo "test"
  - stage: deploy
    script: echo "deploy again"
    name "deploy2"
